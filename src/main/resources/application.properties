#spring.application.name=ProductService
#
#spring.jpa.hibernate.ddl-auto=update
#spring.datasource.url=${productservice_db_url}
#spring.datasource.username=${productservice_db_user}
#spring.datasource.password=${productservice_db_password}
#spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
#spring.jpa.show-sql: true
#server.port=8080

#the above is the option to see the query executed by the jpa



spring.application.name=ProductService

spring.jpa.hibernate.ddl-auto=update
spring.datasource.url=jdbc:mysql://localhost:3306/product_service
spring.datasource.username=product_service_user
#spring.datasource.password=secret
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.show-sql: true
##the above is the option to see the query executed by the jpa

##redis
#spring.data.redis.host
server.port=${PRODUCTSERVICE_PORT}
eureka.client.register-with-eureka=true
eureka.client.fetch-registry=true

# here we don't need to tell the product service where the eureka service is running if it's running on the default port.
